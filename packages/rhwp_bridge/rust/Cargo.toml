[package]
name = "rhwp_bridge"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
rhwp_core = { package = "rhwp", path = "../../../third_party/rhwp" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
